# Быстрый старт

## Запуск проекта

### 1. Остановка существующих контейнеров

```bash
./scripts/stop_containers.sh
```

### 2. Запуск приложения

```bash
docker-compose up -d web
```

Приложение будет доступно на http://localhost:8000

### 3. Запуск тестов

#### Через секретную страницу (рекомендуется):

1. Откройте в браузере: http://localhost:8000/secret-test/?key=test-secret-2024
2. Нажмите нужную кнопку для запуска тестов:
   - **▶ Запустить все тесты** - все тесты
   - **🎬 Запустить сценарные тесты** - только сценарные
   - **🔗 Запустить интеграционные тесты** - интеграционные
   - **🚀 Запустить E2E тесты** - end-to-end тесты

#### Через командную строку:

```bash
# Все тесты
docker-compose run --rm test

# Или через скрипт
./scripts/run_tests.sh
```

## Визуальные тесты (Selenium)

Для визуальных тестов с наблюдением в браузере:

1. Убедитесь, что на хосте установлены:
   - Chrome/Chromium
   - ChromeDriver
   - Python с selenium

2. Запустите визуальные тесты через секретную страницу:
   - Откройте http://localhost:8000/secret-test/?key=test-secret-2024
   - Нажмите **👁️ Запустить визуальные тесты (Selenium)**
   - Браузер откроется автоматически, и вы сможете наблюдать процесс тестирования

## Проверка статуса

```bash
# Проверить статус контейнера
docker-compose ps

# Просмотреть логи
docker-compose logs -f web

# Остановить
docker-compose down
```

## Отчёты тестов

После запуска тестов отчёты доступны в:
- `test-reports/report.html` - HTML отчёт
- `test-reports/coverage/index.html` - Покрытие кода
- `test-results/junit.xml` - JUnit XML


