# Руководство по тестированию

## Секретная страница тестирования

Для запуска сценарных тестов откройте в браузере:

```
http://localhost:8000/secret-test/?key=test-secret-2024
```

## Доступные команды на странице

1. **▶ Запустить все тесты** - запускает все тесты проекта
2. **🎬 Запустить сценарные тесты** - запускает только сценарные тесты (`test_scenarios.py`)
3. **🔗 Запустить интеграционные тесты** - запускает интеграционные тесты (`test_integration.py`)
4. **🚀 Запустить E2E тесты** - запускает end-to-end тесты (`test_e2e_scenarios.py`)
5. **📊 Проверить статус** - проверяет готовность системы тестирования
6. **🗑 Очистить вывод** - очищает область вывода

## Типы тестов

### Сценарные тесты (`test_scenarios.py`)

Покрывают основные сценарии использования:
- Загрузка файла и просмотр результата
- Защита паролем и доступ
- Загрузка нескольких файлов
- Обработка ошибок
- Публичные ссылки
- Работа с разными моделями Whisper

### Интеграционные тесты (`test_integration.py`)

Тестируют полные workflow:
- Полный цикл от загрузки до скачивания
- Работа с сессиями из нескольких файлов
- Восстановление после ошибок

### E2E тесты (`test_e2e_scenarios.py`)

End-to-end сценарии с полным взаимодействием:
- Полный workflow сценарий
- Полный сценарий защиты паролем
- Работа с сессиями
- Обработка ошибок
- Публичные ссылки

## Запуск через командную строку

### Все тесты
```bash
pytest transcribe/tests/ -v
```

### Только сценарные
```bash
pytest transcribe/tests/test_scenarios.py -v -m e2e
```

### Только интеграционные
```bash
pytest transcribe/tests/test_integration.py -v -m integration
```

### Только E2E
```bash
pytest transcribe/tests/test_e2e_scenarios.py -v -m e2e
```

### С отчётом
```bash
pytest transcribe/tests/ --html=test-reports/report.html --self-contained-html --cov=transcribe --cov-report=html:test-reports/coverage
```

## Запуск через Docker

```bash
# Запуск всех тестов
docker-compose run --rm test

# Или через скрипт
./run_tests.sh
```

## Структура тестов

```
transcribe/tests/
├── __init__.py
├── test_models.py          # Тесты моделей
├── test_views.py           # Тесты views
├── test_utils.py           # Тесты утилит
├── test_integration.py     # Интеграционные тесты
├── test_scenarios.py       # Сценарные тесты
└── test_e2e_scenarios.py  # E2E сценарные тесты
```

## Отчёты

После запуска тестов отчёты доступны в:
- `test-reports/report.html` - HTML отчёт
- `test-reports/coverage/index.html` - Покрытие кода
- `test-results/junit.xml` - JUnit XML формат


