# Честная транскрибация переговоров

**Honest Transcription Service - без регистрации и SMS**

Веб-сервис для транскрибации аудио и видео файлов с использованием Whisper (faster-whisper). Позволяет загружать файлы до 500 МБ, автоматически извлекать аудио, транскрибировать текст и при необходимости извлекать скриншоты из видео.

## 🎯 Основные возможности

- **Транскрибация аудио/видео** с использованием различных моделей Whisper
- **Множественная загрузка файлов** (до 500 МБ каждый)
- **Извлечение скриншотов** из видео (1 раз в минуту)
- **Защита паролем** - фраза-пароль для приватных транскрипций
- **Публичные ссылки** для sharing транскрипций
- **Необруталистический дизайн** с адаптивной версткой
- **Без регистрации** - просто загрузи и получи результат
- **Мониторинг диска** - отображение свободного места
- **Яндекс.Метрика** для аналитики

## 🛠 Технологический стек

### Backend
- **Django** - веб-фреймворк
- **faster-whisper** - быстрая реализация Whisper для транскрибации
- **FFmpeg** - извлечение аудио и скриншотов из видео
- **Gunicorn** - WSGI HTTP сервер
- **SQLite** - база данных
- **Nginx** - reverse proxy и статические файлы

### Frontend
- **HTML5/CSS3** - необруталистический дизайн
- **JavaScript** - интерактивность, drag & drop, прогресс-бары
- **Адаптивная верстка** - работает на мобильных устройствах

### Инфраструктура
- **Systemd** - управление сервисами
- **Let's Encrypt/Certbot** - SSL сертификаты
- **SSH** - удаленное управление

## 📋 Установка и настройка

### Требования
- Python 3.10+
- FFmpeg
- Nginx
- Gunicorn
- SSL сертификат (опционально)

### Установка на сервере

1. **Подключение к серверу:**
```bash
ssh root@your-server-ip
```

2. **Установка зависимостей:**
```bash
apt update
apt install -y python3-pip python3-venv ffmpeg nginx certbot python3-certbot-nginx
```

3. **Создание виртуального окружения:**
```bash
python3 -m venv whisper_env
source whisper_env/bin/activate
pip install django faster-whisper gunicorn
```

4. **Настройка Django:**
```bash
cd /root
django-admin startproject whisper_transcribe
cd whisper_transcribe
python manage.py startapp transcribe
```

5. **Миграции базы данных:**
```bash
python manage.py makemigrations
python manage.py migrate
```

6. **Создание суперпользователя:**
```bash
python manage.py createsuperuser
```

7. **Сбор статических файлов:**
```bash
python manage.py collectstatic
```

8. **Настройка Gunicorn:**
Создайте файл `/etc/systemd/system/whisper-transcribe.service`:
```ini
[Unit]
Description=Whisper Transcribe Django Application
After=network.target

[Service]
User=root
Group=root
WorkingDirectory=/root
Environment="PATH=/root/whisper_env/bin"
ExecStart=/root/whisper_env/bin/gunicorn --bind 0.0.0.0:8000 --workers 2 --timeout 300 whisper_transcribe.wsgi:application

[Install]
WantedBy=multi-user.target
```

9. **Запуск сервиса:**
```bash
systemctl daemon-reload
systemctl enable whisper-transcribe.service
systemctl start whisper-transcribe.service
```

10. **Настройка Nginx:**
Создайте конфигурацию в `/etc/nginx/sites-available/whisper-transcribe`:
```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /static/ {
        alias /var/www/staticfiles/;
    }

    location /media/ {
        alias /var/www/media/;
    }
}
```

11. **SSL сертификат:**
```bash
certbot --nginx -d your-domain.com
```

## 🎨 Особенности дизайна

### Необрутализм
- Яркие цвета (#ff6b6b, #6bcf7f, #ffd93d, #4d9de0)
- Толстые черные границы (4px)
- Резкие тени (box-shadow)
- Жирные шрифты (font-weight: 900)
- Анимации текста

### Адаптивность
- Боковое меню на десктопе
- Бургер-меню на мобильных устройствах
- Гибкая верстка (flexbox, grid)
- Адаптивные шрифты (clamp)

## 🔧 Конфигурация

### Модели Whisper
Доступные модели (от быстрой к медленной):
- `tiny` - быстрая, низкое качество
- `base` - баланс (по умолчанию)
- `small` - хорошее качество
- `medium` - высокое качество
- `large-v2` - лучшее качество, медленно
- `large-v3` - лучшее качество, медленно

### Настройки в Django Admin
- Управление транскрипциями
- Просмотр скриншотов
- Генерация публичных токенов
- Фильтрация по статусу, дате, модели

## 📁 Структура проекта

```
/root/
├── whisper_transcribe/          # Django проект
│   ├── settings.py              # Настройки
│   ├── urls.py                  # URL маршруты
│   └── wsgi.py                  # WSGI конфигурация
├── transcribe/                  # Django приложение
│   ├── models.py                # Модели (Transcription, Screenshot)
│   ├── views.py                 # Представления
│   ├── urls.py                  # URL маршруты приложения
│   ├── admin.py                 # Админ-панель
│   └── templates/               # Шаблоны
│       └── transcribe/
│           ├── index.html       # Главная страница
│           └── detail.html      # Страница транскрипции
├── manage.py                    # Django management
├── whisper_env/                 # Виртуальное окружение
└── .gitignore                   # Git ignore файл
```

## 🔐 Безопасность

- **Хеширование паролей** - SHA256 для фраз-паролей
- **CSRF защита** - встроенная в Django
- **Валидация файлов** - проверка размера и типа
- **Очистка временных файлов** - автоматическое удаление после обработки
- **Пароль суперадмина** - для критических операций (очистка диска)

## 📊 Мониторинг

- **Информация о диске** - отображение свободного места
- **Статусы транскрипций** - pending, processing, completed, error
- **Прогресс-бары** - визуализация загрузки и обработки
- **Яндекс.Метрика** - аналитика посещений

## 🚀 Использование

1. **Загрузка файлов:**
   - Перетащите файлы в область загрузки или выберите через диалог
   - Настройте параметры в боковом меню (модель, скриншоты, подпись, пароль)
   - Нажмите "загрузить и транскрибировать"

2. **Просмотр результатов:**
   - Список транскрипций на главной странице
   - Клик по транскрипции для детального просмотра
   - Скачивание текста и скриншотов

3. **Защита паролем:**
   - Укажите фразу-пароль при загрузке
   - Войдите по фразе-паролю для просмотра приватных транскрипций
   - Используйте публичную ссылку с токеном для sharing

## 🔄 Процесс обработки

1. **Загрузка файла** → сохранение во временную директорию
2. **Извлечение аудио** (если видео) → FFmpeg
3. **Извлечение скриншотов** (если включено) → FFmpeg (1 раз в минуту)
4. **Транскрибация** → faster-whisper
5. **Сохранение в БД** → SQLite
6. **Удаление исходного файла** → очистка временных файлов

## 📝 API Endpoints

- `GET /` - главная страница
- `POST /upload/` - загрузка файлов
- `GET /status/<id>/` - статус транскрипции
- `GET /transcription/<id>/` - детали транскрипции
- `GET /public/<token>/` - публичный доступ
- `POST /login-phrase/` - вход по фразе-паролю
- `POST /logout-phrase/` - выход
- `GET /download-text/<id>/` - скачать текст
- `GET /download-screenshots/<id>/` - скачать скриншоты
- `GET /download-session-text/<session>/` - скачать текст сессии

## 🐛 Отладка

### Логи
```bash
journalctl -u whisper-transcribe.service -f
```

### Проверка статуса
```bash
systemctl status whisper-transcribe.service
```

### Перезапуск
```bash
systemctl restart whisper-transcribe.service
```

## 📦 Развертывание

Проект настроен для работы на Linux сервере с:
- Python 3.10+
- Nginx как reverse proxy
- Gunicorn как WSGI сервер
- Systemd для управления сервисом

## 🔍 MCP (Model Context Protocol)

**В данном проекте MCP не используется.**

Проект использует стандартный подход Django для веб-приложения:
- Django ORM для работы с базой данных
- Django Views для обработки запросов
- Django Templates для рендеринга HTML
- faster-whisper для транскрибации (не MCP)

Если вы хотите интегрировать MCP, это потребует:
1. Установку MCP сервера
2. Настройку MCP клиента
3. Интеграцию с Django views
4. Обновление архитектуры для работы с MCP протоколом

## 📄 Лицензия

Проект создан для личного использования. Все права защищены.

## 👤 Автор

Проект разработан для честной транскрибации без регистрации и SMS.

---

**Версия:** 1.0  
**Последнее обновление:** 2024
